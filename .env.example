# .env.example
# ===================================================================
# Bu dosya, Cluster için bir örnek ortam yapılandırma dosyasıdır.
# Kendi ortamınıza göre uyarlamak için bu dosyayı kopyalayın ve adını '.env' olarak değiştirin.
# ===================================================================

# ==============================================================================
# PROJE KİMLİĞİ (MASTER CONFIGURATION)
# ==============================================================================
# Projenin/Şirketin ana adı. Diğer tüm isimlendirmeler buradan türetilir.
PROJECT_NAME="example"
# Projenin kullanacağı üst seviye alan adı (Top-Level Domain).
PROJECT_TLD="cloud"

# ==============================================================================
# FİZİKSEL KÜME TOPOLOJİSİ (CLUSTER TOPOLOGY)
# ==============================================================================
# Bölge A
ZONE_A_HOSTNAME="node-a"
ZONE_A_BACKBONE_IP="100.x.x.a" # Tailscale IP
ZONE_A_PUBLIC_IP="x.x.x.x"   # Public IP
# Bölge B
ZONE_B_HOSTNAME="node-b"
ZONE_B_BACKBONE_IP="100.x.x.b" # Tailscale IP
# Bölge C
ZONE_C_HOSTNAME="node-c"
ZONE_C_BACKBONE_IP="100.x.x.c" # Tailscale IP

# ==============================================================================
# SERVİS TANIMLAMALARI (SERVICE DEFINITIONS & HARMONIC ARCHITECTURE)
# ==============================================================================

# ─── [backbone]: Omurga Servisleri (KATMAN: 5) ───
# IP Bloğu: 10.88.5.X
DISCOVERY_SERVICE_HOST=discovery-service
DISCOVERY_SERVICE_IPV4_ADDRESS=10.88.5.1
DISCOVERY_SERVICE_RPC_PORT=8300 # Server RPC (Remote Procedure Call) portudur.
DISCOVERY_SERVICE_LAN_PORT=8301 # Küme içi Agent iletişimi (Gossip - LAN) (TCP/UDP)
DISCOVERY_SERVICE_WAN_PORT=8302 # Veri merkezleri arası Sunucu iletişimi (Gossip - WAN) (TCP/UDP)
DISCOVERY_SERVICE_HTTP_PORT=8500 # Discovery UI & HTTP API
DISCOVERY_SERVICE_DNS_PORT=8600 # Discovery DNS Arayüzü (TCP/UDP)

# ==============================================================================
# MANTIKSAL KÜME YAPILANDIRMASI (LOGICAL CLUSTER CONFIGURATION)
# ==============================================================================

# Kümenin mantıksal adı. Tüm node'lar aynı veri merkezine aittir.
# Genellikle projenin adı ve ortamı (örn: "example-prod") birleştirilerek oluşturulur.
DISCOVERY_DATACENTER_NAME="${PROJECT_NAME}-main"
DISCOVERY_DOMAIN="${PROJECT_NAME}.${PROJECT_TLD}"
DISCOVERY_SERVICE_SUBDOMAIN="service"
DISCOVERY_DNS_SEARCH_DOMAIN="${DISCOVERY_SERVICE_SUBDOMAIN}.${DISCOVERY_DOMAIN}"
DISCOVERY_SERVICE_HTTP_ADDRESS="http://${DISCOVERY_SERVICE_HOST}:${DISCOVERY_SERVICE_HTTP_PORT}"

# ─── Telefoni Yetenekleri (KATMAN: 30) ───
# IP: 10.88.30.1  =>  Port Bloğu: 1301X
SIP_GATEWAY_HOST=sip-gateway
SIP_GATEWAY_IPV4_ADDRESS=10.88.30.1
SIP_GATEWAY_HTTP_PORT=13010
SIP_GATEWAY_GRPC_PORT=13011
SIP_GATEWAY_METRICS_PORT=13012
SIP_GATEWAY_WS_PORT=13013
SIP_GATEWAY_UDP_PORT=5060

# --- [KATMAN 30]: Telefoni Yetenek Servisleri ---
# IP: 10.88.30.2  =>  Port Bloğu: 1302X
SIP_SIGNALING_HOST=sip-signaling
SIP_SIGNALING_IPV4_ADDRESS=10.88.30.2
SIP_SIGNALING_HTTP_PORT=13020
SIP_SIGNALING_GRPC_PORT=13021
SIP_SIGNALING_METRICS_PORT=13022
SIP_SIGNALING_WS_PORT=13023
SIP_SIGNALING_UDP_PORT=13024
# Uygulamanın DNS üzerinden sorgulayacağı açık isim.
SIP_SIGNALING_DNS_NAME=${SIP_SIGNALING_HOST}.${DISCOVERY_DNS_SEARCH_DOMAIN}
SIP_SIGNALING_REALM="${PROJECT_NAME}_demo"

# ==============================================================================
# DOCKER AĞI VE ÇALIŞMA ORTAMI (NETWORK & RUNTIME)
# ==============================================================================

# --- Docker Ağ Ayarları ---
NETWORK_NAME="${PROJECT_NAME}-net"
NETWORK_SUBNET=10.88.0.0/16
NETWORK_GATEWAY=10.88.0.1

# --- DNS Ayarları ---
PRIMARY_DNS=8.8.8.8 # İnternet erişimi için birincil DNS
SECONDARY_DNS=1.1.1.1 # İnternet erişimi için yedek DNS

# --- Docker Compose Ayarları ---
ENV_FILE_PATH=".env.example"
COMPOSE_HTTP_TIMEOUT=300
DOCKER_REGISTRY=ghcr.io/sentiric # Docker imajlarının çekileceği kayıt deposu
TAG=latest # Kullanılacak imaj etiketi (örn: "latest", "v1.0.0", vs.)

# ==============================================================================
# UYGULAMA DAVRANIŞ ANAHTARLARI (APPLICATION BEHAVIOR)
# ==============================================================================
# Servis keşif yöntemini belirler. Seçenekler: "HTTP" veya "DNS".
DISCOVERY_METHOD="DNS"