{
  "service": {
    "name": "sip-gateway",
    "port": 5060,
    "tags": ["gcp", "udp"],
    "checks": [
      {
        "name": "SIP Gateway HTTP Health Check",
        "http": "http://sip-gateway:5061/health",
        "method": "GET",
        "interval": "10s",
        "timeout": "5s"
      }
    ]
  }
}